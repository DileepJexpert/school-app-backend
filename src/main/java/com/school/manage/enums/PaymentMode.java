package com.school.manage.enums;

/**
 * Enum for different modes of payment.
 * Using an enum is safer and more descriptive than raw strings.
 */
public enum PaymentMode {
    CASH,
    CHEQUE,
    DIGITAL_PAYMENT,
    CHALLAN
}
